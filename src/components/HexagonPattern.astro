---
---

<div class="hexagon-pattern">
  <svg width="100%" height="100%" xmlns="http://www.w3.org/2000/svg">
    <pattern id="hexagonPattern" x="0" y="0" width="50" height="88" patternUnits="userSpaceOnUse">
      <path d="M25,0 L50,14.43 L50,43.3 L25,57.74 L0,43.3 L0,14.43 Z" 
            fill="none" 
            stroke="rgba(79, 70, 229, 0.15)" 
            stroke-width="1.5">
        <animate attributeName="stroke-opacity" 
                 values="0.15;0.3;0.15" 
                 dur="3s" 
                 repeatCount="indefinite"/>
      </path>
    </pattern>
    <rect x="0" y="0" width="100%" height="100%" fill="url(#hexagonPattern)" />
  </svg>
</div>

<style>
  .hexagon-pattern {
    position: absolute;
    inset: 0;
    z-index: -2;
    opacity: 1;
    pointer-events: none;
    transform: scale(1.5);
    animation: rotate 120s linear infinite;
  }

  @keyframes rotate {
    from { transform: scale(1.5) rotate(0deg); }
    to { transform: scale(1.5) rotate(360deg); }
  }
</style>